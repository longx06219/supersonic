apiVersion: v1
kind: Service
metadata:
  name: supersonic-prod
spec:
  selector:
    app: supersonic-prod
  type: ClusterIP
#  type: NodePort
  ports:
  - name: http
#    nodePort: 9080
    port: 9080
    targetPort: 9080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: supersonic-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: supersonic-prod
  template:
    metadata:
      labels:
        app: supersonic-prod
    spec:
      containers:
      - name: supersonic-prod
        image: harbor.wxxinsoft.cn/library/supersonic-prod:0.9.8-SNAPSHOT-1
        # imagePullPolicy: IfNotPresent
        imagePullPolicy: Always
        lifecycle:
          postStart:
            exec:
              command: ["/bin/sh","-c","cd /usr/src/app"]
        ports:
        - name: http
          containerPort: 9080
        volumeMounts:
          - name: dictionary-custom
            mountPath: /usr/src/app/supersonic-standalone-0.9.8-SNAPSHOT/conf/data/dictionary/custom
      volumes:
        - name: dictionary-custom
          nfs:
            server: 192.168.0.159
            path: /data/k8s/factory/supersonic-prod/conf/data/dictionary/custom
